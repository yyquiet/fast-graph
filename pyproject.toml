[project]
name = "fast-graph"
version = "0.1.0"
description = "fast langgraph server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "langchain>=1.2.7",
    "langgraph>=1.0.7",
    "uvicorn>=0.40.0",
]
